node_modules
test/out

# Настоящие треки — это GPS живого человека, вплоть до точки старта у дома.
# В гит не кладём: для прогонов хватает test/fixtures/track.sample.json.
test/fixtures/track.json

# Собранная страница подбора — пересобирается из page/ одной командой.
page/out
